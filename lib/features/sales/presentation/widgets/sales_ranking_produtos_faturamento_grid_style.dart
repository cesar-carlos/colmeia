import 'package:colmeia/shared/design_system/app_data_grid_density.dart';

/// Layout tokens for the ranking faturamento report grid.
/// Header band height for the detailed-rows table (label row + divider).
const double kSalesRankingFaturamentoGridHeaderRowHeight = 36;

/// Fits rank badge, single-line product name, and percent bar + label in one row.
const double kSalesRankingFaturamentoGridDataRowHeight =
    kAppCompactDataRowHeight;

const double kSalesRankingFaturamentoRankBadgeSize = 28;

/// Default chart canvas height when the section is not height-constrained.
const double kSalesRankingFaturamentoPieChartHeight = 300;

/// Title + spacing above the chart (used to fit within a fixed outer slot).
const double kSalesRankingFaturamentoPieTitleBandHeight = 32;
