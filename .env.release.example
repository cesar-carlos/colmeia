# Copy to `.env.release` in the repository root (gitignored). Used by
# `installer/build_installer.py` to pass AUTO_UPDATE_FEED_URL into
# `flutter build windows --release`.
#
# Official feed (same as GitHub Raw appcast on main):
AUTO_UPDATE_FEED_URL=https://raw.githubusercontent.com/cesar-carlos/colmeia/main/appcast.xml
